description = "Onboard an existing project into the workshop"
prompt = "Use the project-lifecycle skill to onboard an existing project from the path: {{args}}. Scan the repo, detect the tech stack, and register it in the workshop."
