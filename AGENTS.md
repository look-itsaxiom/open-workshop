# Open Workshop — Codex Edition (Repo Root)

If you are running Codex from the repo root, prefer the Codex edition:
- Start Codex in `codex/` so `.agents/skills/` and `AGENTS.md` are auto-loaded.
- The Codex edition uses the same `~/.open-workshop/` data as Claude.

If you stay at the repo root, follow the same instructions in `codex/AGENTS.md`.
